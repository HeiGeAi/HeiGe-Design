# 大理石纹（marble-vein · 奢侈高定）— Compact

> An architectural luxury system quarried from a single slab. Warm Carrara white (#f0ede8) is the field, onyx charcoal is the ink, and one cool slate vein is spent like a fault line — a drawn crack, never text. Cinzel Roman inscriptional capitals stand over a Space Grotesk drafting sans and IBM Plex Mono dimension callouts, so the type reads carved rather than typeset. The signature move is one real irregular vein that becomes the layout's structural spine, registered to a faint facade grid. One token set drives both an interface and a 16:9 keynote. Sharp corners, no gradient, depth by stone value and a single hairline.

**Theme：** light

## 关键色

| 角色 | Token | Hex |
|---|---|---|
| 主色 | `primary` | `#1f2228` |
| 正文 | `ink` | `#2a2d33` |
| 底色 | `canvas` | `#f0ede8` |
| 卡片面 | `surface` | `#f6f4ef` |
| 次要文字 | `muted` | `#5f636b` |
| 分隔线 | `hairline` | `#d8d3c9` |
| 辅助 | `accent` | `#828b97` |
| 辅助 | `panel` | `#181a1e` |

## 字体

- 标题：`Cinzel`
- 正文：`Space Grotesk`
- 数字 / 等宽：`IBM Plex Mono`

## 形状 & 间距

- 圆角：none 0px / sm 3px / pill 9999px
- 间距：xs 4px / sm 8px / md 16px / lg 24px / xl 40px / section 96px

## 用前先读（5 条铁律）

1. 主色 `#1f2228` 只给关键 CTA 与强调，克制使用。
2. 复现完整 DESIGN.md 里 `## Signature Moment` 写死的签名时刻。
3. 中文字体栈末尾必带系统兜底，中文不用日文字体渲染。
4. 不碰紫蓝俗套渐变，不做居中卡片三件套。
5. 交付前对照 `## Do's and Don'ts` 自查，跑 `heige-design lint marble-vein` 确认 0 error。

> 完整设定（组件规格 / 图像语言 / 演示系统 / Agent 配方卡）见同目录 `DESIGN.md`。
