# 制表局（grid-bureau · 瑞士纪律）— Compact

> Grid Bureau is Swiss institutional order pushed to its authoritarian extreme: a neutral grayscale field governed by exactly one functional certification blue (#2757d6), set in Inter with every figure spoken in monospace. Everything snaps to an 8px baseline grid that is left exposed, not hidden. The signature is a numbered statute gutter (§1.0 / §1.1) and a dark monospace filing stamp that registers each block like an official public record. Original system by HeiGe-Design.

**Theme：** light

## 关键色

| 角色 | Token | Hex |
|---|---|---|
| 主色 | `primary` | `#2757d6` |
| 正文 | `ink` | `#1a1d24` |
| 底色 | `canvas` | `#f4f5f7` |
| 卡片面 | `surface` | `#ffffff` |
| 次要文字 | `muted` | `#5b6270` |
| 分隔线 | `hairline` | `#d5d8dd` |
| 辅助 | `grid` | `#e4e6ea` |
| 辅助 | `block` | `#14171d` |

## 字体

- 标题：`Inter`
- 正文：`Inter`
- 数字 / 等宽：`IBM Plex Mono`

## 形状 & 间距

- 圆角：none 0px / sm 2px / pill 9999px
- 间距：xs 4px / sm 8px / md 16px / lg 24px / xl 48px / section 96px

## 用前先读（5 条铁律）

1. 主色 `#2757d6` 只给关键 CTA 与强调，克制使用。
2. 复现完整 DESIGN.md 里 `## Signature Moment` 写死的签名时刻。
3. 中文字体栈末尾必带系统兜底，中文不用日文字体渲染。
4. 不碰紫蓝俗套渐变，不做居中卡片三件套。
5. 交付前对照 `## Do's and Don'ts` 自查，跑 `heige-design lint grid-bureau` 确认 0 error。

> 完整设定（组件规格 / 图像语言 / 演示系统 / Agent 配方卡）见同目录 `DESIGN.md`。
