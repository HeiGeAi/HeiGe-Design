# 亚麻褶（linen-fold · 有机自然）— Compact

> A sustainable natural-beauty system grounded in oatmeal linen, deep sage green, and a terracotta clay accent. Humanist Fraunces serif carries the craft; a handwritten kicker adds a human hand. The signature is physical, not digital — procedurally torn deckle paper edges, single-stroke botanical line drawings, and a woven linen texture ground. Never straight machine cuts, never gloss, never gradients.

**Theme：** light

## 关键色

| 角色 | Token | Hex |
|---|---|---|
| 主色 | `primary` | `#5c6b45` |
| 正文 | `ink` | `#2e2a20` |
| 底色 | `canvas` | `#e7ddc9` |
| 卡片面 | `surface` | `#f3ecdd` |
| 次要文字 | `muted` | `#635b48` |
| 分隔线 | `hairline` | `#d6c9ae` |
| 辅助 | `accent` | `#8f5333` |

## 字体

- 标题：`Fraunces`
- 正文：`Mulish`
- 数字 / 等宽：`Spline Sans Mono`

## 形状 & 间距

- 圆角：none 0px / sm 6px / md 12px / lg 20px / pill 9999px
- 间距：xs 4px / sm 8px / md 16px / lg 24px / xl 40px / section 96px

## 用前先读（5 条铁律）

1. 主色 `#5c6b45` 只给关键 CTA 与强调，克制使用。
2. 复现完整 DESIGN.md 里 `## Signature Moment` 写死的签名时刻。
3. 中文字体栈末尾必带系统兜底，中文不用日文字体渲染。
4. 不碰紫蓝俗套渐变，不做居中卡片三件套。
5. 交付前对照 `## Do's and Don'ts` 自查，跑 `heige-design lint linen-fold` 确认 0 error。

> 完整设定（组件规格 / 图像语言 / 演示系统 / Agent 配方卡）见同目录 `DESIGN.md`。
