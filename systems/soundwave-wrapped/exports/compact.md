# 声浪（soundwave-wrapped · 张扬极繁）— Compact

> Soundwave (声浪) is a maximalist year-in-review system for music and campaign recaps, loud on purpose. Fluorescent pink

**Theme：** dark

## 关键色

| 角色 | Token | Hex |
|---|---|---|
| 主色 | `primary` | `#FF2D95` |
| 正文 | `ink` | `#F7F4EC` |
| 底色 | `canvas` | `#0E0C14` |
| 卡片面 | `surface` | `#1B1826` |
| 次要文字 | `muted` | `#ADA8BC` |
| 分隔线 | `hairline` | `#2E2B3C` |
| 辅助 | `volt` | `#FFE500` |
| 辅助 | `cobalt` | `#1E4DFF` |

## 字体

- 标题：`Anton`
- 正文：`Inter`
- 数字 / 等宽：`Space Mono`

## 形状 & 间距

- 圆角：none 0px / sm 3px / md 6px / lg 14px / pill 9999px
- 间距：xs 4px / sm 8px / md 16px / lg 24px / xl 40px / section 80px

## 用前先读（5 条铁律）

1. 主色 `#FF2D95` 只给关键 CTA 与强调，克制使用。
2. 复现完整 DESIGN.md 里 `## Signature Moment` 写死的签名时刻。
3. 中文字体栈末尾必带系统兜底，中文不用日文字体渲染。
4. 辉光只用 box-shadow / radial-gradient，不用 backdrop-filter，不动画化阴影。
5. 交付前对照 `## Do's and Don'ts` 自查，跑 `heige-design lint soundwave-wrapped` 确认 0 error。

> 完整设定（组件规格 / 图像语言 / 演示系统 / Agent 配方卡）见同目录 `DESIGN.md`。
