# 陶土（clayfield · 有机自然）— Compact

> Clayfield is a hand-thrown, sun-baked wellness identity. Fired terracotta is the load-bearing brand color set against oat cream, with sage green as its garden companion. A rounded humanist sans (Nunito) carries the everyday voice while a warm serif (Fraunces) punctuates it at eyebrows, pull-quotes and hero numerals. Every image is masked to a unique thrown-clay blob over a paper-grain field — never a rounded rectangle, never a drop-shadow, never a purple gradient.

**Theme：** light

## 关键色

| 角色 | Token | Hex |
|---|---|---|
| 主色 | `primary` | `#a8482a` |
| 正文 | `ink` | `#3a2e26` |
| 底色 | `canvas` | `#f2e9dc` |
| 卡片面 | `surface` | `#fbf5ea` |
| 次要文字 | `muted` | `#6f5f52` |
| 分隔线 | `hairline` | `#e2d5c2` |
| 辅助 | `clay` | `#c96f4a` |
| 辅助 | `sage` | `#9caf88` |

## 字体

- 标题：`Nunito`
- 正文：`Nunito`
- 数字 / 等宽：`Fraunces`

## 形状 & 间距

- 圆角：sm 8px / md 16px / lg 28px / xl 44px / pill 9999px
- 间距：xs 4px / sm 8px / md 16px / lg 24px / xl 40px / section 96px

## 用前先读（5 条铁律）

1. 主色 `#a8482a` 只给关键 CTA 与强调，克制使用。
2. 复现完整 DESIGN.md 里 `## Signature Moment` 写死的签名时刻。
3. 中文字体栈末尾必带系统兜底，中文不用日文字体渲染。
4. 不碰紫蓝俗套渐变，不做居中卡片三件套。
5. 交付前对照 `## Do's and Don'ts` 自查，跑 `heige-design lint clayfield` 确认 0 error。

> 完整设定（组件规格 / 图像语言 / 演示系统 / Agent 配方卡）见同目录 `DESIGN.md`。
