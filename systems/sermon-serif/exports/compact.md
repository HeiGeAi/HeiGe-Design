# 布道（sermon-serif · 优雅编辑）— Compact

> House style of a fictional editorial design studio where the serif itself is the argument. Warm paper (#faf7f2) carries a warm near-black ink and one restrained ochre; a light optical serif sets giant ragged-right pull-quotes against clean grotesque body text, with hanging mono folios and text that wraps around a single figure. Column grids, deep negative space, no gradients. One token set drives both a portfolio site and a 16:9 keynote. Ochre is used like a signature — once per view, never as fill.

**Theme：** light

## 关键色

| 角色 | Token | Hex |
|---|---|---|
| 主色 | `primary` | `#1a1a18` |
| 正文 | `ink` | `#23201b` |
| 底色 | `canvas` | `#faf7f2` |
| 卡片面 | `surface` | `#f2ebde` |
| 次要文字 | `muted` | `#6c645a` |
| 分隔线 | `hairline` | `#ddd4c4` |
| 辅助 | `accent` | `#935428` |

## 字体

- 标题：`Fraunces`
- 正文：`Inter`
- 数字 / 等宽：`IBM Plex Mono`

## 形状 & 间距

- 圆角：none 0px / sm 2px / md 6px / pill 9999px
- 间距：xs 4px / sm 8px / md 16px / lg 24px / xl 40px / section 80px

## 用前先读（5 条铁律）

1. 主色 `#1a1a18` 只给关键 CTA 与强调，克制使用。
2. 复现完整 DESIGN.md 里 `## Signature Moment` 写死的签名时刻。
3. 中文字体栈末尾必带系统兜底，中文不用日文字体渲染。
4. 不碰紫蓝俗套渐变，不做居中卡片三件套。
5. 交付前对照 `## Do's and Don'ts` 自查，跑 `heige-design lint sermon-serif` 确认 0 error。

> 完整设定（组件规格 / 图像语言 / 演示系统 / Agent 配方卡）见同目录 `DESIGN.md`。
