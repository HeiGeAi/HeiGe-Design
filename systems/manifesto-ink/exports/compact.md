# 墨宣言（manifesto-ink · 优雅编辑）— Compact

> Redline is the home turf of an opinion columnist. A heavy Fraunces serif is slammed in warm-black onto warm ivory paper, and a single cinnabar red is rationed for one weapon only: the hand-drawn underline that marks the sentence you are meant to remember. Running prose is set in a reading serif; datelines, folios, and buttons are set in mono, like the machinery of the press. Stark rectangles, almost no rounding, one argument per screen. Chinese falls back to Noto Serif SC / Songti SC, never a Japanese face.

**Theme：** light

## 关键色

| 角色 | Token | Hex |
|---|---|---|
| 主色 | `primary` | `#15110a` |
| 正文 | `ink` | `#1d1710` |
| 底色 | `canvas` | `#f7f4ee` |
| 卡片面 | `surface` | `#efe8db` |
| 次要文字 | `muted` | `#6d6353` |
| 分隔线 | `hairline` | `#d8cfbe` |
| 辅助 | `accent` | `#c42718` |

## 字体

- 标题：`Fraunces`
- 正文：`Newsreader`
- 数字 / 等宽：`IBM Plex Mono`

## 形状 & 间距

- 圆角：none 0px / sm 2px / pill 999px
- 间距：xs 4px / sm 8px / md 16px / lg 24px / xl 40px / section 96px

## 用前先读（5 条铁律）

1. 主色 `#15110a` 只给关键 CTA 与强调，克制使用。
2. 复现完整 DESIGN.md 里 `## Signature Moment` 写死的签名时刻。
3. 中文字体栈末尾必带系统兜底，中文不用日文字体渲染。
4. 不碰紫蓝俗套渐变，不做居中卡片三件套。
5. 交付前对照 `## Do's and Don'ts` 自查，跑 `heige-design lint manifesto-ink` 确认 0 error。

> 完整设定（组件规格 / 图像语言 / 演示系统 / Agent 配方卡）见同目录 `DESIGN.md`。
