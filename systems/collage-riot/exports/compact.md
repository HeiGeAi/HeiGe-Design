# 拼贴暴动（collage-riot · 张扬极繁）— Compact

> A ransom-note collage system for music-festival sponsorship pitches and street-culture events. Four process inks — fluoro pink, process cyan, marker yellow, key black — clash on warm newsprint like a poster torn off a wall and re-pasted. Headlines are cut-and-spliced across Anton and Archivo Black, anchored by rotated masking-tape stickers and hard static offset shadows. Sharp cut edges, bare column rules, mono date-stamps. Loud, printed, physical. Nothing here reads as a template — it reads as glue, scissors, and a photocopier at 2am.

**Theme：** light

## 关键色

| 角色 | Token | Hex |
|---|---|---|
| 主色 | `primary` | `#FF1F8E` |
| 正文 | `ink` | `#17140F` |
| 底色 | `canvas` | `#EDE7D8` |
| 卡片面 | `surface` | `#F7F2E8` |
| 次要文字 | `muted` | `#5E5749` |
| 分隔线 | `hairline` | `#26221B` |
| 辅助 | `accent` | `#00AEEF` |
| 辅助 | `highlight` | `#FFE500` |

## 字体

- 标题：`Anton`
- 正文：`Space Grotesk`
- 数字 / 等宽：`Space Mono`

## 形状 & 间距

- 圆角：none 0px / sm 2px / md 4px / lg 8px / pill 9999px
- 间距：xs 4px / sm 8px / md 16px / lg 24px / xl 40px / section 80px

## 用前先读（5 条铁律）

1. 主色 `#FF1F8E` 只给关键 CTA 与强调，克制使用。
2. 复现完整 DESIGN.md 里 `## Signature Moment` 写死的签名时刻。
3. 中文字体栈末尾必带系统兜底，中文不用日文字体渲染。
4. 不碰紫蓝俗套渐变，不做居中卡片三件套。
5. 交付前对照 `## Do's and Don'ts` 自查，跑 `heige-design lint collage-riot` 确认 0 error。

> 完整设定（组件规格 / 图像语言 / 演示系统 / Agent 配方卡）见同目录 `DESIGN.md`。
