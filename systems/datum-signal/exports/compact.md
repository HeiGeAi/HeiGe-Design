# 数点（datum-signal · 瑞士纪律）— Compact

> Datum Signal is a Swiss-disciplined analytics system for BI dashboards and reporting products. Near-white canvas and near-black ink carry all structure; a single Signal Cobalt lights only the one number that matters, while tabular monospace figures and delta green/red report change. Strict baseline grid, hairline dividers, pixel-true tables, near-square corners. Hierarchy is built from size and spacing, never decoration. Signature: one immaculate chart with a single lit datum, and a decimal-locked stat hero where every digit snaps to the grid.

**Theme：** light

## 关键色

| 角色 | Token | Hex |
|---|---|---|
| 主色 | `primary` | `#1f4dff` |
| 正文 | `ink` | `#15171c` |
| 底色 | `canvas` | `#fbfbfa` |
| 卡片面 | `surface` | `#ffffff` |
| 次要文字 | `muted` | `#565c65` |
| 分隔线 | `hairline` | `#e6e6e4` |
| 辅助 | `up` | `#15803d` |
| 辅助 | `down` | `#c62828` |

## 字体

- 标题：`Inter`
- 正文：`Inter`
- 数字 / 等宽：`IBM Plex Mono`

## 形状 & 间距

- 圆角：none 0px / sm 3px / md 10px
- 间距：xs 4px / sm 8px / md 16px / lg 24px / xl 40px / section 80px

## 用前先读（5 条铁律）

1. 主色 `#1f4dff` 只给关键 CTA 与强调，克制使用。
2. 复现完整 DESIGN.md 里 `## Signature Moment` 写死的签名时刻。
3. 中文字体栈末尾必带系统兜底，中文不用日文字体渲染。
4. 不碰紫蓝俗套渐变，不做居中卡片三件套。
5. 交付前对照 `## Do's and Don'ts` 自查，跑 `heige-design lint datum-signal` 确认 0 error。

> 完整设定（组件规格 / 图像语言 / 演示系统 / Agent 配方卡）见同目录 `DESIGN.md`。
