# 磁带未来（cassette-futurism · 复古未来）— Compact

> Cassette-futurism system for FERRITE, a fictional hardware house that imagines the future the way 1979 did: molded beige plastic lit by a single amber readout. Chassis orange #d9662a runs the physical push-buttons; amber #ffb347 is caged inside dark readout windows and never touches the beige. Space Grotesk stamps the faceplate legends, Space Mono drives the seven-segment counters. The signature move is the power-on readout, where oversized amber digits breathe over an engraved beige panel.

**Theme：** light

## 关键色

| 角色 | Token | Hex |
|---|---|---|
| 主色 | `primary` | `#d9662a` |
| 正文 | `ink` | `#241f17` |
| 底色 | `canvas` | `#e8e0cf` |
| 卡片面 | `surface` | `#f1ebdc` |
| 次要文字 | `muted` | `#5e5544` |
| 分隔线 | `hairline` | `#c9bfa8` |
| 辅助 | `amber` | `#ffb347` |
| 辅助 | `readout` | `#141009` |

## 字体

- 标题：`Space Grotesk`
- 正文：`Space Grotesk`
- 数字 / 等宽：`Space Mono`

## 形状 & 间距

- 圆角：none 0px / sm 3px / md 7px / lg 12px / pill 9999px
- 间距：xs 4px / sm 8px / md 16px / lg 24px / xl 40px / section 88px

## 用前先读（5 条铁律）

1. 主色 `#d9662a` 只给关键 CTA 与强调，克制使用。
2. 复现完整 DESIGN.md 里 `## Signature Moment` 写死的签名时刻。
3. 中文字体栈末尾必带系统兜底，中文不用日文字体渲染。
4. 不碰紫蓝俗套渐变，不做居中卡片三件套。
5. 交付前对照 `## Do's and Don'ts` 自查，跑 `heige-design lint cassette-futurism` 确认 0 error。

> 完整设定（组件规格 / 图像语言 / 演示系统 / Agent 配方卡）见同目录 `DESIGN.md`。
